Last updated: 10/20/2025


algorithms:

NSGA-II has a redundant population declaration in the run command; it should only have the number of children and parents. 
MOPAC PM7 and PM6 have result inconsistencies when running with NSGA-II as opposed to a single calculation in quantum_chemistry.
Rank tracking is stored in the molecule database JSON file for NSGA-II and is pegged to 999999.
NSGA-II has significant memory consumption over time when using DFT functionals, causing memory swap and CPU utilization instability (resulting in worse performance over time).
SA has issues extracting the correct beta mean from quantum_chemistry, needs to have the same flow as NSGA-II, with parsing output and extracting beta mean.
Hypervolume for NSGA-II is still in progress.


quantum_chemistry:

xTB-based methods are still in progress, NOT ready for use.
Coupled Cluster is lacking additional property calculations; it just has the beta mean.
